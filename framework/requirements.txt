# Core ML packages (PyTorch installed separately with CUDA support)
# torch is PINNED to the golden-fixture / executorch-toolchain version (2.12.0): the DeComFL golden
# fixtures + executorch==1.3.1 native extension were built against it, and test_torch_version_matches_manifest
# gates on it. torchvision/torchaudio are intentionally NOT listed here: the framework does not import
# them (setup.py already filters torch*/torchvision* out of install_requires), and pulling torchvision
# from PyPI against this pytorch-index torch build produces an ABI mismatch (`operator torchvision::nms
# does not exist`) that breaks transformers-importing tests. Install them from the matched pytorch CPU
# index alongside torch only where a consumer actually needs them.
torch==2.12.0

# Transformers and NLP
transformers==4.55.2
datasets==3.1.0
tokenizers==0.21.4
peft>=0.11

# Deep Learning utilities
accelerate==1.10.0
safetensors==0.6.2

# Data processing
numpy==2.1.2
pandas==2.3.1
pyarrow

# Networking (updated grpcio version)
grpcio>=1.75.1
grpcio-health-checking>=1.62.3
aiohttp>=3.14.0,<4.0  # CVE-2026-34993/47265 (RCE) + DoS/smuggling fixes
aiohttp-cors==0.8.1
aiohappyeyeballs==2.6.1
aiosignal==1.4.0
requests>=2.33.0,<3.0  # CVE-2026-25645

# Security — FL-boundary connection-token verification (SE-1). Vetted JWT lib for a security gate
# (alg-confusion / none-alg handled) rather than hand-rolled JWS parsing.
PyJWT>=2.8,<3

# Google Cloud / Monitoring
google-api-core==2.25.1
google-auth==2.40.3
googleapis-common-protos==1.70.0
opencensus==0.11.4
opencensus-context==0.1.3
opentelemetry-api==1.36.0
opentelemetry-exporter-prometheus==0.57b0
opentelemetry-proto==1.27.0
opentelemetry-sdk==1.36.0
opentelemetry-semantic-conventions==0.57b0
prometheus_client

# Ray (distributed computing)
ray

# Visualization (relaxed versions for Python 3.10 compatibility)
matplotlib>=3.7,<3.10
seaborn>=0.12,<0.14
contourpy>=1.2.0,<1.3  # Python 3.10 compatible version

# GPU monitoring
nvidia-ml-py==12.575.51
pynvml==12.0.0

# System utilities
psutil>=5.9,<8.0
click==8.1.8
colorama==0.4.6
colorful==0.5.7
typer>=0.9,<0.13
rich>=13.0,<14.0
tqdm>=4.65,<5.0

# Cryptography
# PYSEC-2026-35 (x2) + CVE-2026-26007 are fixed in 46.0.5+. flwr is no longer a
# dependency, so its cryptography<45.0.0 cap is gone — pin to the patched 46.x line.
cryptography>=46.0.6,<47.0
pycryptodome>=3.19,<4.0

# File handling
pillow>=12.2.0,<13.0  # PYSEC-2026-165 + CVE-2026-40192/42309/42310/42311 (the <12.0 cap sat below the fix)
PyYAML>=6.0,<7.0

# Data serialization
# The floor tracks the NEWEST gencode among the committed stubs, not a preference: protobuf requires
# runtime >= gencode, and fot_pb2.py is generated at 5.29.0 (fedlearn_pb2.py at 4.25.1). The previous
# <5.0.0 cap therefore produced a clean install that could not import fot_pb2 — i.e. the FoT servicer,
# fot_server, and the fl_fot_server.py entry point the backend spawns. It went unnoticed because the
# FoT path imports it lazily and the gradient path never does. Pinned by tests/test_protobuf_gencode_pin.py.
# NOTE: the backend and client-docker lockfiles still cap protobuf <5.0.0 via flwr/flwr-datasets, so the
# FoT path remains uninstallable THERE — a second flwr-induced residual alongside SE-22 (cryptography).
protobuf>=5.29.0,<6.0.0
googleapis-common-protos==1.70.0
msgpack>=1.0,<2.0
dill>=0.3.7,<0.4
multiprocess>=0.70,<0.71

# Utilities
filelock>=3.12,<4.0
fsspec>=2024.2,<2025.0
huggingface-hub>=0.20,<1.0
regex>=2023.0,<2026.0
xxhash>=3.0,<4.0
smart_open>=6.4,<8.0

# Type checking and validation
pydantic>=2.5,<3.0
pydantic_core>=2.14,<3.0
typing-inspection>=0.4,<0.5
typing_extensions>=4.8,<5.0

# Other dependencies
certifi>=2023.0
charset-normalizer>=3.0,<4.0
idna>=3.4,<4.0
urllib3>=2.0,<3.0
packaging>=23.0,<26.0
six>=1.16,<2.0
python-dateutil>=2.8,<3.0
pytz>=2023.0
tzdata>=2023.0

# Build tools
setuptools
wheel
scikit-learn
